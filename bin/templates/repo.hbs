import Repo from "@repos/Repo";
import {{{pascalCase modelName}}}, { I{{{pascalCase modelName}}} } from "@models/{{{pascalCase modelName}}}";

class {{{pascalCase modelName}}}Repo extends Repo<I{{{pascalCase modelName}}}> {
  constructor() {
    super({{{pascalCase modelName}}});
  }
}

export default new {{{pascalCase modelName}}}Repo();
